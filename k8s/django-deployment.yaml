apiVersion: apps/v1
kind: Deployment
metadata:
  name: savannahshop-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: savannahshop-web
  template:
    metadata:
      labels:
        app: savannahshop-web
    spec:
      containers:
        - name: savannahshop-web
          image: savannahshop:latest
          ports:
            - containerPort: 8000
          env:
            - name: DJANGO_SETTINGS_MODULE
              value: savannahshop.settings
          # Add other env vars as needed
